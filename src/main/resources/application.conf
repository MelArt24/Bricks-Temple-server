ktor {
    deployment {
        port = ${?PORT}
    }

    application {
        modules = [ com.brickstemple.ApplicationKt.module ]
    }

    database {
        url = ${?DB_URL}
        driver = "org.postgresql.Driver"
        user = ${?DB_USER}
        password = ${?DB_PASSWORD}
    }
}
